## Packages
(none needed)

## Notes
- Base64 file upload logic is implemented directly in the frontend before submitting to the creation endpoint.
- Tailwind config assumes CSS variables for fonts (--font-sans).
- Added a layout wrapper for mobile PWA feel (bottom navigation).
